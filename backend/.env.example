# Copy this file to .env and fill in the values before running the server.

# Groq
GROQ_API_KEY=gsk_...

# CORS — set to your deployed frontend URL in production
FRONTEND_URL=http://localhost:3000

# Server (used by the start command on Render)
HOST=0.0.0.0
PORT=8000
